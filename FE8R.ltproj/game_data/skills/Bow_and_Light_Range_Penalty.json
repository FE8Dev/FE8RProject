[
    {
        "nid": "Bow_and_Light_Range_Penalty",
        "name": "Bow and Light Range Penalty <icon>skill_icons 7, 40</>",
        "desc": "-25 Hit with Bows and Light magic for every additional space beyond 2 range.",
        "icon_nid": "skill_icons",
        "icon_index": [
            7,
            40
        ],
        "components": [
            [
                "hidden",
                null
            ],
            [
                "condition",
                "item_system.weapon_type(unit, item) == 'Bow', 'Light'"
            ],
            [
                "dynamic_accuracy",
                "-25 * (utils.calculate_distance(unit.position, target.position) -2) if target and utils.calculate_distance(unit.position, target.position) > 2 and utils.calculate_distance(unit.position, target.position) in item_funcs.get_range(unit, unit.get_weapon()) else 0"
            ]
        ]
    }
]